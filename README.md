# wallet-mpc-tss
A secure, production-ready implementation of a multi-party computation (MPC) wallet using Threshold Signature Schemes (TSS). This library enables decentralized key management and transaction signing without ever reconstructing the full private key, significantly reducing single points of failure and attack surface.
